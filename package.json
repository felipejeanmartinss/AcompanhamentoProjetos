{
  "name": "acompanhamento-projetos",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "serve": "node scripts/server.mjs",
    "test": "node --test tests/*.test.js",
    "check": "node --check src/app.js && node --check src/domain.js && node --check src/importer.js && node --check src/store.js",
    "validate": "npm run check && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
